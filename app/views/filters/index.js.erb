function replaceResults() {
  const newColumns = `<%= render 'cards/cards', columns: @columns%>`
  document.querySelector("#row-for-columns").innerHTML = newColumns
  const teamInfo = document.getElementById('team-info');
  teamInfo.innerHTML = `<%= @columns[0].teams[0].name.capitalize %>`
}
replaceResults()
